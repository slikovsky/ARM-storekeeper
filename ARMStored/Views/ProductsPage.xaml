<UserControl x:Class="ARMStored.Views.ProductsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" 
             d:DesignWidth="900">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Text="📋 Управление товарами" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       Foreground="#FF333333"
                       VerticalAlignment="Center"/>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Width="250"
                         Height="35"
                         Padding="10,5"
                         BorderBrush="#FFCCCCCC"
                         BorderThickness="1"
                         VerticalContentAlignment="Center"/>
                <Button Content="🔍" 
                        Width="35" 
                        Height="35"
                        Margin="5,0,0,0"
                        Background="#FF2196F3"
                        Foreground="White"
                        BorderThickness="0"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Button Content="➕ Добавить" 
                    Command="{Binding AddCommand}"
                    Style="{StaticResource PrimaryButton}"
                    Margin="0,0,10,0"/>
            <Button Content="✏️ Редактировать" 
                    Command="{Binding EditCommand}"
                    Background="#FFFF9800"
                    Foreground="White"
                    Padding="15,8"
                    BorderThickness="0"
                    Margin="0,0,10,0"/>
            <Button Content="💾 Сохранить" 
                    Command="{Binding SaveCommand}"
                    Background="#FF4CAF50"
                    Foreground="White"
                    Padding="15,8"
                    BorderThickness="0"
                    Margin="0,0,10,0"/>
            <Button Content="🗑️ Удалить" 
                    Command="{Binding DeleteCommand}"
                    Background="#FFE74C3C"
                    Foreground="White"
                    Padding="15,8"
                    BorderThickness="0"
                    Margin="0,0,10,0"/>
            <Button Content="🔄 Обновить" 
                    Command="{Binding RefreshCommand}"
                    Background="#FF9E9E9E"
                    Foreground="White"
                    Padding="15,8"
                    BorderThickness="0"/>
        </StackPanel>

        <Border Grid.Row="2" 
                Background="White" 
                CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
            </Border.Effect>
            
            <DataGrid ItemsSource="{Binding Products}"
                      SelectedItem="{Binding SelectedProduct}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      GridLinesVisibility="Horizontal"
                      BorderThickness="0"
                      HeadersVisibility="Column"
                      RowHeight="40"
                      AlternatingRowBackground="#FFF5F5F5">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Артикул" Binding="{Binding Article}" Width="100"/>
                    <DataGridTextColumn Header="Категория" Binding="{Binding CategoryName}" Width="120"/>
                    <DataGridTextColumn Header="Цена" Binding="{Binding Price, StringFormat=C}" Width="100"/>
                    <DataGridTextColumn Header="Кол-во" Binding="{Binding Quantity}" Width="80"/>
                    <DataGridTextColumn Header="Ед. изм." Binding="{Binding Unit}" Width="80"/>
                    <DataGridTemplateColumn Header="Статус" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding StatusColor}" 
                                        CornerRadius="12" 
                                        Padding="8,3"
                                        HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding Status}" 
                                               Foreground="White" 
                                               FontSize="11"
                                               FontWeight="SemiBold"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="2" 
                Background="#FFFFFFFF" 
                CornerRadius="8"
                Padding="20"
                Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}"
                VerticalAlignment="Bottom"
                Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="3" Opacity="0.2"/>
            </Border.Effect>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="Название *" FontSize="11" Foreground="#FF666666"/>
                    <TextBox Text="{Binding SelectedProduct.Name}" Height="30" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,10,0">
                    <TextBlock Text="Артикул" FontSize="11" Foreground="#FF666666"/>
                    <TextBox Text="{Binding SelectedProduct.Article}" Height="30" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Штрихкод" FontSize="11" Foreground="#FF666666"/>
                    <TextBox Text="{Binding SelectedProduct.Barcode}" Height="30" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,10,10,0">
                    <TextBlock Text="Категория" FontSize="11" Foreground="#FF666666"/>
                    <ComboBox ItemsSource="{Binding Categories}"
                              SelectedValue="{Binding SelectedProduct.CategoryId}"
                              SelectedValuePath="Id"
                              DisplayMemberPath="Name"
                              Height="30" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,10,10,0">
                    <TextBlock Text="Цена" FontSize="11" Foreground="#FF666666"/>
                    <TextBox Text="{Binding SelectedProduct.Price}" Height="30" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,10,0,0">
                    <TextBlock Text="Количество" FontSize="11" Foreground="#FF666666"/>
                    <TextBox Text="{Binding SelectedProduct.Quantity}" Height="30" Margin="0,5,0,0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>